export class Timeset {
    hr:number;
    min:number;
    sec:number; 

    constructor(hr:number, min:number, sec:number){
        this.hr = hr;
        this.min = min;
        this.sec = sec; 
    }
}